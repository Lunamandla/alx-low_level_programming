#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - programme to print last digit
 *
 * Return: Always (Success)
 */
int main(void)
{
	int n = 5,0,6;

	if (n > 5)
	{
		printf("n and is greater than 5\n" )
	}
	else if (n == 0)
	{
		printf("n and is 0\n" )
	}
	else (n < 6 != 0)
	{
		printf("n and is less than 6 and not 0\n" )
	}
	return (0);
}
