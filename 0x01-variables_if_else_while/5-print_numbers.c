#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Programme that prints single numbers from zero to ten
 *
 * Return: Always (Success)
 */
int main(void)
{
	int a;

	for (a = 0;  a < 10; a++)
	{

	printf("\n %d", a);

	}
	return (0);
}

