#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main -  programme that prints alphabets lowercase and uppercase
 *
 * Return: Always (Success)
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++);
	printf("%c \n", a);
	for (i = 'A'; i <= 'Z'; i++);
	printf("%c\n", i)
	return (0);
}
