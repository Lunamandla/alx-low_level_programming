#include "main.h"
/**
 * _isupper - check if the letter is uppercase
 *
 * Return: 1 for uppercase and 0 for anything else
 */
int _isupper(int a)
{
	if (a >= 65 && a <= 90)
	{
	return (1);
	}
	return (0);
}
